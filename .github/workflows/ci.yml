name: CI

# When to run
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel in-progress runs when new commit pushed
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: Lint, Type Check, Test
    runs-on: ubuntu-latest

    steps:
      # 1. Clone the repository
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Install pnpm (must match packageManager in package.json)
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.28.2

      # 3. Set up Node.js (must match engines in package.json)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      # 4. Install dependencies
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # 5. Generate Prisma client (needed for type-safe lint & typecheck)
      #    Uses a dummy URL because generate only reads the schema, no DB connection needed
      - name: Generate Prisma client
        run: pnpm --filter @decksmith/db db:generate
        env:
          DATABASE_URL: 'postgresql://localhost:5432/dummy'

      # 6. Check formatting (fail if not formatted)
      - name: Check formatting
        run: pnpm format:check

      # 7. Run linter
      - name: Lint
        run: pnpm lint

      # 8. Type check
      - name: Type check
        run: pnpm typecheck

      # 9. Run tests (when they exist)
      # - name: Test
      #   run: pnpm test
