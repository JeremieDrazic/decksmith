Foundational surface with tactile depth — `surface` fill, subtle border, inner highlight. Set
`interactive` for the hover lift + accent glow used on tiles and list rows.

```jsx
<Card>…</Card>
<Card interactive padding="18px">…</Card>
```

Props: `interactive`, `padding`. Compose freely; don't nest interactive cards.
